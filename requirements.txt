# requirements.txt — Core dependencies for RAN KPI Anomaly Detection
# Pinned to minimum versions per whitepaper §11 Python Dependencies table.
# Install: pip install -r requirements.txt
# Verify:  pip install --dry-run -r requirements.txt

pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0
scikit-learn>=1.3.0
scipy>=1.11.0
matplotlib>=3.7.0
seaborn>=0.12.0
